<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.ReaderActivity">

    <Button
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:id="@+id/back"
        android:layout_margin="16dp"
        android:background="@drawable/back_hitam"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/card_scan"
        android:id="@+id/img"
        android:layout_centerInParent="true"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/scanhere_tv"
        android:text="Scan Kartu Mahasiswa"
        android:layout_above="@id/img"
        android:textAlignment="center"
        android:fontFamily="@font/raleway"
        android:textSize="20sp"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:id="@+id/textview"
        android:layout_below="@id/img"
        android:gravity="center_horizontal"
        android:text="Contoh"/>

    <Button
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:text="Cek Status"
        android:id="@+id/cek"
        android:textAllCaps="false"
        android:layout_marginBottom="16dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/bg_item_jadwal"
        android:fontFamily="@font/raleway"/>

</RelativeLayout>